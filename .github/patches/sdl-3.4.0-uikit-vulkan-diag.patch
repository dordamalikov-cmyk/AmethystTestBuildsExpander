diff --git a/src/video/uikit/SDL_uikitvulkan.m b/src/video/uikit/SDL_uikitvulkan.m
index 860b298..85751b9 100644
--- a/src/video/uikit/SDL_uikitvulkan.m
+++ b/src/video/uikit/SDL_uikitvulkan.m
@@ -211,6 +211,14 @@ bool UIKit_Vulkan_CreateSurface(SDL_VideoDevice *_this,
         return false;
     }

+    /* [Amethyst diag] one-shot: Vulkan surface creation - confirms the SDL
+     * window exists and owns a CAMetalLayer at this instant (diagnostic for the
+     * black-screen composition issue). fprintf goes to stderr, which the app
+     * redirects to its log; SDL_Log is not captured there. */
+    fprintf(stderr, "[SDL Vulkan] UIKit_Vulkan_CreateSurface window='%s' flags=0x%x\n",
+            window->title, window->flags);
+    fflush(stderr);
+
     if (vkCreateMetalSurfaceEXT) {
         VkMetalSurfaceCreateInfoEXT createInfo = {};
         createInfo.sType = VK_STRUCTURE_TYPE_METAL_SURFACE_CREATE_INFO_EXT;
